version: '3.8'

services:
  bilibili-video-info:
    build: .
    container_name: bilibili-video-info
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - .:/app
    command: node src/index.js