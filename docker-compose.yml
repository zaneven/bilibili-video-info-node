version: '3.8'

services:
  bilibili-video-info:
    build: .
    container_name: bilibili-video-info
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    command: node src/index.js